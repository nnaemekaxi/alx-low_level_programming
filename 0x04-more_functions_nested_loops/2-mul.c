#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two integers
 *@a: multiply
 *@b: multiply
 *
 * Return: 0
 */

int mul(int a, int b)
{
	int a;
	int b;
	int c;

	c = (a * b);
	printf("%d\n", c);
	return (0);
}
