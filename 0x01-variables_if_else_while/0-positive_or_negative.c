#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
	{int n;
		if(n>0) {
			printf(n, " is positive");
		} if(n==) {
			printf(n, "is zero");
		} else {
			printf(n, "is negative");
		}
	}
