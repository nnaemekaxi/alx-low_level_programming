#include <stdio.h>
#include "main.h"

/**
 * _islower - checks for lower case c
 *
 * Return: 1
 */

int _islower(int c)
{
	int c;
	char l;
	int l;

	l = getchar();
	if (l == 99)
	{
		return (1);
	}
	else
	{
		return (0);
}
	return (0);
}
